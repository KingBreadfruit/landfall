# Supabase configuration for Landfall
# Copy this file to .env (or .env.local) and fill in the values below.
# Both values are safe to expose in the browser: the anon key is public and
# Row Level Security protects the data.

VITE_SUPABASE_URL=https://iqxdrtgklcthfhoicocd.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
